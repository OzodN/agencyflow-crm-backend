spring:
  application:
    name: agencyflow-crm

  datasource:
    url: jdbc:postgresql://localhost:5432/agencyflow
    username: agencyflow
    password: agencyflow

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

jwt:
  secret: 6f9e5d0a8c9f4b0f8a1c7d6e5f4a3b2c1d9e8f7a6b5c4d3e2f1a0b9c8d7e6f5
  expiration: 86400000